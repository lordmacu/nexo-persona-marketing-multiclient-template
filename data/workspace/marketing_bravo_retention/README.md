# Workspace — marketing_bravo_retention

Per-agent scratch dir. The runtime never reads from this unless
your customized `system_prompt` references files here.

Suggested:
- `brand-voice.md` — tone-of-voice the agent matches
- `escalation-criteria.md` — what triggers a human handoff
- `templates/` — canned responses the agent can paste
